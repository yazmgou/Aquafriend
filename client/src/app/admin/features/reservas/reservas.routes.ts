import { Routes } from '@angular/router';
import { ReservasComponent } from './reservas.component';

export const reservasRoutes: Routes = [
  { path: '', component: ReservasComponent, title: 'Reservas' }
];
